CREATE TABLE user (
                       user_id INT PRIMARY KEY AUTO_INCREMENT,
                       user_name VARCHAR(255),
                       email VARCHAR(255),
                       password VARCHAR(255),
                       role VARCHAR(50)
);

